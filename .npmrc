# npm registry
registry=https://registry.npmjs.org/

# Authentication is handled via NPM_TOKEN environment variable
# Set NPM_TOKEN in your shell profile or CI environment
# export NPM_TOKEN=npm_xxxxx
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
